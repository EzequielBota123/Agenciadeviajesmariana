node_modules
.next
out
.env
.env.local
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.DS_Store

.vercel
.env*

# Metadata local de Claude Code / plugins de agente (no del proyecto)
.agents/
.claude/
skills-lock.json

# Worker: node_modules y navegador de Playwright son pesados, no van al repo
worker/node_modules

worker/worker.log
worker/*.log
